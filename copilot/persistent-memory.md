# Copilot Persistent Memory

* Avoid Cloudflare APIs; prefer Worker Script API independently.
* Angular defaults: separation of concerns, LTS, Angular Material.
* Prefer database-query, backend-first implementation when it makes sense.
* Angular TS6+ needs explicit rootDir in tsconfig.app.json and tsconfig.spec.json when outDir is set.
* Use hash-tag URL notation for Angular on Cloudflare.
